@model IEnumerable<ThAmCo.Events.Models.GuestViewModels.GuestVM>

<h3>All Guests</h3>

<p>
    <a asp-action="CreateGuest">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="EditGuest" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="GuestDetails" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteGuest" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
